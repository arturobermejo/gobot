package model

import (
	"testing"
)

func TestDataLoader(t *testing.T) {
	// inData, outData, inVocab, outVocab := GetData("../data/train")

	// x := EncodeInputs(inData, inVocab, 0)
	// y := EncodeOutputs(outData, outVocab)
	// // fmt.Println(x)
	// // fmt.Println(y)
}
